{
  "AllowMultiple": "false",
  "Description": "\"Add LLM Auto-Mapping step to database read/write nodes in executeWorkflow.\"",
  "EndLine": "2494",
  "Instruction": "\"Integrate LLM mapping translation for DB nodes.\"",
  "ReplacementContent": "\"                } else if (currentNode.data.model === 'db-read' || currentNode.data.model === 'db-write') {\\n                    // --- DATABASE NODES ---\\n                    const isRead = currentNode.data.model === 'db-read';\\n                    const table = currentNode.data.tableName || '';\\n                    addLog('info', `🗄️ Database ${isRead ? 'Read' : 'Write'}: ${table || 'no table'}`, currentNode.id);\\n                    setNodes(prev => prev.map(n => n.id === currentNode.id ? { ...n, data: { ...n.data, isExecuting: true } } : n));\\n                    try {\\n                        let mappedInput = inputContext;\\n                        if (currentNode.data.llmAutoMap) {\\n                            addLog('info', `✨ LLM Auto-Mapping context for ${currentNode.data.label}...`, currentNode.id);\\n                            try {\\n                                const systemPrompt = currentNode.data.llmAutoMapPrompt || 'You are a data mapping assistant. Output a JSON object or array representing the row to insert.';\\n                                const provider = currentNode.data.llmAutoMapProvider || 'groq';\\n                                const model = currentNode.data.llmAutoMapModel || '';\\n                                const apiKey = currentNode.data.llmAutoMapApiKey || '';\\n                                mappedInput = await generateAgentResponse(\\n                                    inputContext || 'No input context',\\n                                    systemPrompt,\\n                                    model,\\n                                    undefined,\\n                                    apiKey,\\n                                    provider\\n                                );\\n                                addLog('info', `✨ LLM Auto-Mapped result: \\\"${mappedInput.substring(0, 100)}...\\\"`, currentNode.id);\\n                            } catch (llmMapErr: any) {\\n                                addLog('warning', `✨ LLM Auto-Mapping failed: ${llmMapErr.message}. Using r\n<truncated 923 bytes>",
  "StartLine": "2477",
  "TargetContent": "\"                } else if (currentNode.data.model === 'db-read' || currentNode.data.model === 'db-write') {\\n                    // --- DATABASE NODES ---\\n                    const isRead = currentNode.data.model === 'db-read';\\n                    const table = currentNode.data.tableName || '';\\n                    addLog('info', `🗄️ Database ${isRead ? 'Read' : 'Write'}: ${table || 'no table'}`, currentNode.id);\\n                    setNodes(prev => prev.map(n => n.id === currentNode.id ? { ...n, data: { ...n.data, isExecuting: true } } : n));\\n                    try {\\n                        const resp = await fetch('http://localhost:8080/api/v1/database/execute', {\\n                            method: 'POST',\\n                            headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer demo-token' },\\n                            body: JSON.stringify({\\n                                operation: isRead ? 'select' : (currentNode.data.dbOperation || 'insert'),\\n                                table, filter: currentNode.data.dbFilter, limit: currentNode.data.dbLimit,\\n                                data: isRead ? undefined : inputContext,\\n                                dbType: currentNode.data.dbType || 'sqlite',\\n                                connectionString: currentNode.data.connectionString || '',\\n                            })\\n                        });\"",
  "TargetFile": "\"c:\\\\Users\\\\bisha\\\\Downloads\\\\aether\\\\Aether-workflow\\\\components\\\\Builder.tsx\"",
  "toolAction": "\"Adding LLM mapping to database execution block\"",
  "toolSummary": "\"Editing Builder.tsx to add LLM mapping to DB node execution\""
}