{
  "AllowMultiple": "false",
  "Description": "\"Update frontend execution fetch calls for DuckDuckGo search and RSS feed reader to pass the node's custom LLM settings.\"",
  "EndLine": "2566",
  "Instruction": "\"Add llmProvider, llmModel, llmApiKey, llmTemperature, and systemPrompt parameters to the fetch body payload of ddg-search and rss-reader execution blocks.\"",
  "ReplacementContent": "\"                } else if (currentNode.data.model === 'ddg-search') {\\n                    // --- DUCKDUCKGO SEARCH ---\\n                    addLog('info', `Searching DuckDuckGo (RAG Loop)...`, currentNode.id);\\n                    setNodes(prev => prev.map(n => n.id === currentNode.id ? { ...n, data: { ...n.data, isExecuting: true } } : n));\\n                    try {\\n                        const query = currentNode.data.searchQuery || inputContext || 'hello';\\n                        const resp = await fetch(`http://localhost:8080/api/v1/integrations/ddg/search`, {\\n                            method: 'POST',\\n                            headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer demo-token' },\\n                            body: JSON.stringify({ \\n                                query,\\n                                systemPrompt: currentNode.data.systemPrompt,\\n                                llmProvider: currentNode.data.llmProvider,\\n                                llmModel: currentNode.data.llmModel,\\n                                llmApiKey: currentNode.data.llmApiKey,\\n                                llmTemperature: currentNode.data.llmTemperature\\n                            })\\n                        });\\n                        const result = await resp.json();\\n                        if (result.success) {\\n                            const output = result.data?.answer || result.answer || '';\\n                            const passForward = result.data?.passForward !== false;\\n                            const justification = result.data?.justification || '';\\n                            \\n                            executionResults.set(currentNode.id, output);\\n                            passForwardMap.set(currentNode.id, passForward);\\n                            \\n                            setNodes(prev => prev.map(n => n.id === currentNode.id ? { ...n, data: { ...n.data, output, isExecuting: false } } : n));\\n                            addLog('success', `Duc\n<truncated 3272 bytes>",
  "StartLine": "2507",
  "TargetContent": "\"                } else if (currentNode.data.model === 'ddg-search') {\\n                    // --- DUCKDUCKGO SEARCH ---\\n                    addLog('info', `Searching DuckDuckGo (RAG Loop)...`, currentNode.id);\\n                    setNodes(prev => prev.map(n => n.id === currentNode.id ? { ...n, data: { ...n.data, isExecuting: true } } : n));\\n                    try {\\n                        const query = currentNode.data.searchQuery || inputContext || 'hello';\\n                        const resp = await fetch(`http://localhost:8080/api/v1/integrations/ddg/search`, {\\n                            method: 'POST',\\n                            headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer demo-token' },\\n                            body: JSON.stringify({ query })\\n                        });\\n                        const result = await resp.json();\\n                        if (result.success) {\\n                            const output = result.data?.answer || result.answer || '';\\n                            const passForward = result.data?.passForward !== false;\\n                            const justification = result.data?.justification || '';\\n                            \\n                            executionResults.set(currentNode.id, output);\\n                            passForwardMap.set(currentNode.id, passForward);\\n                            \\n                            setNodes(prev => prev.map(n => n.id === currentNode.id ? { ...n, data: { ...n.data, output, isExecuting: false } } : n));\\n                            addLog('success', `DuckDuckGo RAG complete. Resolved: ${passForward ? 'Yes' : 'No'}`, currentNode.id);\\n                            if (justification) {\\n                                addLog('info', `Evaluation justification: ${justification}`, currentNode.id);\\n                            }\\n                        } else {\\n                            throw new Error(result.error || 'Search query optimization or verification failed.');\\n                  \n<truncated 2356 bytes>",
  "TargetFile": "\"c:\\\\Users\\\\bisha\\\\Downloads\\\\aether\\\\Aether-workflow\\\\components\\\\Builder.tsx\"",
  "toolAction": "\"Updating frontend execution fetch calls for search and RSS\"",
  "toolSummary": "\"Update executeWorkflow in Builder.tsx for RSS/DDG\""
}